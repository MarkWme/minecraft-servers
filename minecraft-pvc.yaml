apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraftserver-disk
  namespace: minecraft
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 5Gi